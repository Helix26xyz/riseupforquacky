# riseupforquacky
How could they do this!?

This repository is deployed to GitHub Pages with the workflow at `.github/workflows/deploy-pages.yml`.
Pushes to `main` automatically publish the static site, and you can also trigger a manual deployment with `workflow_dispatch`.
